WindowManager:
    StartMenu:
    MainWindow:
    SecondWindow:
    ThirdWindow:
    FourthWindow:
    FifthWindow:
    SixthWindow:
    SeventhWindow:
    EighthWindow:
    NinthWindow:

<StartMenu>:
    name: "start"

    BoxLayout:
        orientation: "vertical"
        size: root.width, root.height

        BoxLayout:
            #cols:2

            Label:
                text: "Welcome to the Mask Quiz. Click Start to find your perfect mask! "


        Button:
            text: "Start"
            background_color: 1,0,0,1
            pos_hint: {"x":.25, "y":.4}
            size_hint: 0.5, 0.2
            on_release:
                app.root.current = "main"
                root.manager.transition.direction = "up"


<MainWindow>:
    name: "main"

    BoxLayout:
        orientation: "vertical"
        size: root.width, root.height

        padding: 50
        spacing: 50
        #cols:1

        BoxLayout:
            #cols:2

            Label:
                text: "What kind of mask would you like? "

        Button:
            text: "Disposable"
            background_color: 0.4, 0.2, 1, 1
            pos_hint: {"x":.25, "y":.4}
            size_hint: 0.5, 0.2
            on_press: root.clk(*args)
            on_release:
                app.root.current = "third"
                root.manager.transition.direction = "left"

        Button:
            text: "Reusable"
            background_color: 1, 1, 1, 0.6
            pos_hint: {"x":.25, "y":.4}
            size_hint: 0.5, 0.2
            on_press: root.clk(*args)
            on_release:
                app.root.current = "second"
                root.manager.transition.direction = "left"


<SecondWindow>:
    name: "second"

    BoxLayout:
        orientation: "vertical"
        size: root.width, root.height

        padding: 50
        spacing: 50

        GridLayout:
            cols:2

            Label:
                text: "What material of mask do you prefer?"

        Button:
            text: "Cotton"
            background_color: 1, 1, 1, 0.6
            pos_hint: {"x":.25, "y":.4}
            size_hint: 0.5, 0.2
            on_press: root.clk(*args)
            on_release:
                app.root.current = "third"
                root.manager.transition.direction = "left"

        Button:
            text: "Polyester"
            background_color: 1, 1, 1, 0.6
            pos_hint: {"x":.25, "y":.4}
            size_hint: 0.5, 0.2
            on_press: root.clk(*args)
            on_release:
                app.root.current = "third"
                root.manager.transition.direction = "left"

        Button:
            text: "Spandex"
            background_color: 1, 1, 1, 0.6
            pos_hint: {"x":.25, "y":.4}
            size_hint: 0.5, 0.2
            on_press: root.clk(*args)
            on_release:
                app.root.current = "third"
                root.manager.transition.direction = "left"

        Button:
            text: "Does not matter"
            background_color: 1, 1, 1, 0.6
            pos_hint: {"x":.25, "y":.4}
            size_hint: 0.5, 0.2
            on_press: root.clk(*args)
            on_release:
                app.root.current = "third"
                root.manager.transition.direction = "left"

        Button:
            background_color:1,0,0,1
            pos_hint: {"x":.25, "y":.4}
            size_hint: 0.5, 0.2
            text: "Back"
            on_release:
                app.root.current = "main"
                root.manager.transition.direction = "right"

<ThirdWindow>:
    name: "third"

    BoxLayout:
        orientation: "vertical"
        size: root.width, root.height

        padding: 50
        spacing: 50

        GridLayout:
            cols: 2

            Label:
                text: "What size mask would you like?"

        Button:
            text: "Childrens"
            background_color: 1, 1, 1, 0.6
            pos_hint: {"x":.25, "y":.4}
            size_hint: 0.5, 0.2
            on_press: root.clk(*args)
            on_release:
                app.root.current = "fourth"
                root.manager.transition.direction = "left"

        Button:
            text: "Adult"
            background_color: 1, 1, 1, 0.6
            pos_hint: {"x":.25, "y":.4}
            size_hint: 0.5, 0.2
            on_press: root.clk(*args)
            on_release:
                app.root.current = "fourth"
                root.manager.transition.direction = "left"

        Button:
            text: "Small"
            background_color: 1, 1, 1, 0.6
            pos_hint: {"x":.25, "y":.4}
            size_hint: 0.5, 0.2
            on_press: root.clk(*args)
            on_release:
                app.root.current = "fourth"
                root.manager.transition.direction = "left"

        Button:
            text: "Medium"
            background_color: 1, 1, 1, 0.6
            pos_hint: {"x":.25, "y":.4}
            size_hint: 0.5, 0.2
            on_press: root.clk(*args)
            on_release:
                app.root.current = "fourth"
                root.manager.transition.direction = "left"

        Button:
            text: "Large"
            background_color: 1, 1, 1, 0.6
            pos_hint: {"x":.25, "y":.4}
            size_hint: 0.5, 0.2
            on_press: root.clk(*args)
            on_release:
                app.root.current = "fourth"
                root.manager.transition.direction = "left"

        Button:
            text: "X-Large"
            background_color: 1, 1, 1, 0.6
            pos_hint: {"x":.25, "y":.4}
            size_hint: 0.5, 0.2
            on_press: root.clk(*args)
            on_release:
                app.root.current = "fourth"
                root.manager.transition.direction = "left"

        Button:
            text: "Universal"
            background_color: 1, 1, 1, 0.6
            pos_hint: {"x":.25, "y":.4}
            size_hint: 0.5, 0.2
            on_press: root.clk(*args)
            on_release:
                app.root.current = "fourth"
                root.manager.transition.direction = "left"

        Button:
            background_color:1,0,0,1
            pos_hint: {"x":.25, "y":.4}
            size_hint: 0.5, 0.2
            text: "Back"
            on_release:
                app.root.current = "second"
                root.manager.transition.direction = "right"

<FourthWindow>:
    name: "fourth"

    BoxLayout:
        orientation: "vertical"
        size: root.width, root.height

        padding: 50
        spacing: 50

        GridLayout:
            cols: 2

            Label:
                text: "Would you like a male female or unisex mask?"

        Button:
            text: "Male"
            background_color: 0.4, 0.2, 1, 1
            pos_hint: {"x":.25, "y":.4}
            size_hint: 0.5, 0.2
            on_press: root.clk(*args)
            on_release:
                app.root.current = "fifth"
                root.manager.transition.direction = "left"

        Button:
            text: "Female"
            background_color: 0.4, 0.2, 1, 1
            pos_hint: {"x":.25, "y":.4}
            size_hint: 0.5, 0.2
            on_press: root.clk(*args)
            on_release:
                app.root.current = "fifth"
                root.manager.transition.direction = "left"

        Button:
            text: "Unisex"
            background_color: 0.4, 0.2, 1, 1
            pos_hint: {"x":.25, "y":.4}
            size_hint: 0.5, 0.2
            on_press: root.clk(*args)
            on_release:
                app.root.current = "fifth"
                root.manager.transition.direction = "left"

        Button:
            background_color:1,0,0,1
            pos_hint: {"x":.25, "y":.4}
            size_hint: 0.5, 0.2
            text: "Back"
            on_release:
                app.root.current = "third"
                root.manager.transition.direction = "right"


<FifthWindow>:
    name: "fifth"

    BoxLayout:
        orientation: "vertical"
        size: root.width, root.height

        padding: 50
        spacing: 50


        GridLayout:
            cols: 2

            Label:
                text: "Would color mask would you like?"

        Button:
            text: "Red"
            background_color: 0.4, 0.2, 1, 1
            pos_hint: {"x":.25, "y":.4}
            size_hint: 0.5, 0.2
            on_press: root.clk(*args)
            on_release:
                app.root.current = "sixth"
                root.manager.transition.direction = "left"

        Button:
            text: "Orange"
            background_color: 0.4, 0.2, 1, 1
            pos_hint: {"x":.25, "y":.4}
            size_hint: 0.5, 0.2
            on_press: root.clk(*args)
            on_release:
                app.root.current = "sixth"
                root.manager.transition.direction = "left"

        Button:
            text: "Yellow"
            background_color: 0.4, 0.2, 1, 1
            pos_hint: {"x":.25, "y":.4}
            size_hint: 0.5, 0.2
            on_press: root.clk(*args)
            on_release:
                app.root.current = "sixth"
                root.manager.transition.direction = "left"

        Button:
            text: "Green"
            background_color: 0.4, 0.2, 1, 1
            pos_hint: {"x":.25, "y":.4}
            size_hint: 0.5, 0.2
            on_press: root.clk(*args)
            on_release:
                app.root.current = "sixth"
                root.manager.transition.direction = "left"

        Button:
            text: "Blue"
            background_color: 0.4, 0.2, 1, 1
            pos_hint: {"x":.25, "y":.4}
            size_hint: 0.5, 0.2
            on_press: root.clk(*args)
            on_release:
                app.root.current = "sixth"
                root.manager.transition.direction = "left"

        Button:
            text: "Purple"
            background_color: 0.4, 0.2, 1, 1
            pos_hint: {"x":.25, "y":.4}
            size_hint: 0.5, 0.2
            on_press: root.clk(*args)
            on_release:
                app.root.current = "sixth"
                root.manager.transition.direction = "left"

        Button:
            text: "Pattern"
            background_color: 0.4, 0.2, 1, 1
            pos_hint: {"x":.25, "y":.4}
            size_hint: 0.5, 0.2
            on_press: root.clk(*args)
            on_release:
                app.root.current = "sixth"
                root.manager.transition.direction = "left"

        Button:
            text: "Black"
            background_color: 0.4, 0.2, 1, 1
            pos_hint: {"x":.25, "y":.4}
            size_hint: 0.5, 0.2
            on_press: root.clk(*args)
            on_release:
                app.root.current = "sixth"
                root.manager.transition.direction = "left"


        Button:
            text: "Grey"
            background_color: 0.4, 0.2, 1, 1
            pos_hint: {"x":.25, "y":.4}
            size_hint: 0.5, 0.2
            on_press: root.clk(*args)
            on_release:
                app.root.current = "sixth"
                root.manager.transition.direction = "left"


        Button:
            background_color:1,0,0,1
            pos_hint: {"x":.25, "y":.4}
            size_hint: 0.5, 0.2
            text: "Back"
            on_release:
                app.root.current = "fourth"
                root.manager.transition.direction = "right"


<SixthWindow>:
    name: "sixth"

    BoxLayout:
        orientation: "vertical"
        size: root.width, root.height

        padding: 50
        spacing: 50


        GridLayout:
            cols: 2

            Label:
                text: "What level of filtration would you like?"

        Button:
            text: "Low"
            background_color: 0.4, 0.2, 1, 1
            pos_hint: {"x":.25, "y":.4}
            size_hint: 0.5, 0.2
            on_press: root.clk(*args)
            on_release:
                app.root.current = "seventh"
                root.manager.transition.direction = "left"

        Button:
            text: "Medium"
            background_color: 0.4, 0.2, 1, 1
            pos_hint: {"x":.25, "y":.4}
            size_hint: 0.5, 0.2
            on_press: root.clk(*args)
            on_release:
                app.root.current = "seventh"
                root.manager.transition.direction = "left"

        Button:
            text: "High"
            background_color: 0.4, 0.2, 1, 1
            pos_hint: {"x":.25, "y":.4}
            size_hint: 0.5, 0.2
            on_press: root.clk(*args)
            on_release:
                app.root.current = "seventh"
                root.manager.transition.direction = "left"


        Button:
            background_color:1,0,0,1
            pos_hint: {"x":.25, "y":.4}
            size_hint: 0.5, 0.2
            text: "Back"
            on_release:
                app.root.current = "fifth"
                root.manager.transition.direction = "right"

<SeventhWindow>:
    name: "seventh"

    BoxLayout:
        orientation: "vertical"
        size: root.width, root.height

        padding: 50
        spacing: 50


        GridLayout:
            cols: 2

            Label:
                text: "Do you want elastic or adjustable?"

        Button:
            text: "Elastic"
            background_color: 0.4, 0.2, 1, 1
            pos_hint: {"x":.25, "y":.4}
            size_hint: 0.5, 0.2
            on_press: root.clk(*args)
            on_release:
                app.root.current = "eighth"
                root.manager.transition.direction = "left"

        Button:
            text: "Adjustable"
            background_color: 0.4, 0.2, 1, 1
            pos_hint: {"x":.25, "y":.4}
            size_hint: 0.5, 0.2
            on_press: root.clk(*args)
            on_release:
                app.root.current = "eighth"
                root.manager.transition.direction = "left"

        Button:
            background_color:1,0,0,1
            pos_hint: {"x":.25, "y":.4}
            size_hint: 0.5, 0.2
            text: "Back"
            on_release:
                app.root.current = "sixth"
                root.manager.transition.direction = "right"


<EighthWindow>:
    name: "eighth"

    BoxLayout:
        orientation: "vertical"
        size: root.width, root.height

        padding: 50
        spacing: 50


        GridLayout:
            cols: 2

            Label:
                text: "Thank you for completing those questions. Click Finish! to view your results"

        Button:
            background_color:1,0,0,1
            pos_hint: {"x":.25}
            size_hint: 0.5, 0.2
            text: "Finish!"
            on_press: root.clk(*args)
            on_release:
                app.root.current = "ninth"
                root.manager.transition.direction = "up"


<NinthWindow>:
    name: "ninth"
    Button:
        background_color:0,1,1,0

        text: 'Text'
        pos_hint: {"x":.25, "y":.4}
        size_hint: 0.5, 0.2
        on_press: root.clk(*args)
